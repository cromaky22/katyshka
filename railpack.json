{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["npm ci"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["true"]
    }
  },
  "deploy": {
    "inputs": [{ "step": "install" }],
    "startCommand": "npm start"
  }
}
